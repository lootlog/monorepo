apiVersion: v1
kind: Service
metadata:
  name: admin
  labels:
    app: admin
    service: admin
  annotations:
    ingress.kubernetes.io/service-upstream: "true"
spec:
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
      name: http
  selector:
    app: admin
