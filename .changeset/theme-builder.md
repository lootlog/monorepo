---
"@lootlog/api": minor
"@lootlog/api-client": minor
"@lootlog/types": minor
"@lootlog/ui": minor
"@lootlog/web": minor
---

Add a versioned theme library, atomic theme settings API, custom theme import and export, reusable component recipes, and a full-screen Lootlog theme builder with live application preview. Finalize the version 1 contract with explicit component, typography, chart, surface, shadow, and interaction states; scope portaled UI to the preview canvas; and remove implicit generic component colors. Migrate legacy theme preferences into appearance settings, use a quieter surface ramp for standard presets and newly generated themes, and preserve special Rukia, Rias, and cat theme packages behind generic UI decoration slots.

Replace the builder's generic demo canvas with faithful personal and organization shells, shared dashboard presentation views, realistic loot fixtures, simulated responsive viewports, and a focus mode that expands the preview without saving the draft.
