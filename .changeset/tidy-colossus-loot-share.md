---
"@lootlog/api": patch
---

Make loot-share updates idempotent, allow chat-confirmed allocations to replace inferred item ownership, and initialize validated standard-colossus shares during loot creation.
