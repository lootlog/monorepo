{
  "name": "@lootlog/wiki",
  "version": "0.1.3",
  "private": true,
  "license": "MIT",
  "type": "module",
  "sideEffects": false,
  "imports": {
    "#/*": "./src/*"
  },
  "scripts": {
    "dev": "vite dev",
    "build": "vite build && tsc --noEmit",
    "preview": "vite preview",
    "deploy": "pnpm build && wrangler deploy dist/server/index.js --config wrangler.jsonc --env=\"\" --assets dist/client",
    "deploy:develop": "pnpm build && wrangler deploy dist/server/index.js --config wrangler.jsonc --env develop --assets dist/client",
    "deploy:version": "pnpm build && wrangler versions upload dist/server/index.js --config wrangler.jsonc --env=\"\" --assets dist/client",
    "deploy:version:develop": "pnpm build && wrangler versions upload dist/server/index.js --config wrangler.jsonc --env develop --assets dist/client",
    "cloudflare:typegen": "wrangler types",
    "lint": "oxlint .",
    "test": "vitest run --passWithNoTests --config ./vitest.config.ts"
  },
  "dependencies": {
    "@lootlog/api-client": "workspace:*",
    "@lootlog/ui": "workspace:*",
    "@tanstack/react-query": "catalog:",
    "@tanstack/react-query-devtools": "catalog:",
    "@tanstack/react-router": "catalog:",
    "@tanstack/react-start": "^1.168.32",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.51.0",
    "@tailwindcss/vite": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "tailwindcss": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:",
    "wrangler": "^4.119.0"
  }
}
