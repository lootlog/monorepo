# API Services
VITE_API_URL=http://localhost/api/lootlog
VITE_GATEWAY_URL=http://localhost
VITE_AUTH_SERVICE_URL=http://localhost/api/auth
VITE_BATTLELOG_API_URL=http://localhost/api/battlelog

VITE_LOOTLOG_APP_URL=http://localhost

# WebSocket Gateway
VITE_GATEWAY_SOCKET_PATH=/gateway/ws

# Optional local override. On Cloudflare Workers Builds this is populated
# automatically via WORKERS_CI_COMMIT_SHA during the Vite build.
VITE_COMMIT_SHA=
